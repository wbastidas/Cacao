<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- El tema real lo define Compose en ui/Tema.kt. Este solo evita el
         parpadeo blanco al abrir la app antes de que Compose tome el control. -->
    <style name="Theme.CacaoTrace" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/background_light</item>
    </style>
</resources>
