# CacaoTrace — dependencias del pipeline de entrenamiento
#
# Instalación:  pip install -r requirements.txt
# En Google Colab con GPU T4 ya vienen casi todas; el pip install solo completa.

tensorflow>=2.16,<2.21     # clasificadores M1, M3, M4 y exportación a TFLite
scikit-learn>=1.3          # reporte de precisión/recall y matriz de confusión
matplotlib>=3.7            # curvas de entrenamiento y matriz de confusión
pillow>=10.0               # lectura de fotos, corrección de rotación EXIF
numpy>=1.24
pyyaml>=6.0                # data.yaml del detector

# Detector de granos M2 (prueba de corte).
# AVISO DE LICENCIA: Ultralytics se distribuye bajo AGPL-3.0. Si vas a distribuir
# un modelo entrenado con él dentro de un producto comercial, revisa sus condiciones
# o adquiere la licencia empresarial de Ultralytics.
ultralytics>=8.3

# Solo para las pruebas automáticas del pipeline
pytest>=7.4
